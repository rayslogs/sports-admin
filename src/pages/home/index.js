import React from 'react'
import PropTypes from 'prop-types'

const Home = props => {
  return <div>首页</div>
}

Home.propTypes = {
  model: PropTypes.object,
}

Home.defaultProps = {
  model: {},
}

export default Home
